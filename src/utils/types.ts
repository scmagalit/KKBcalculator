export type item = {
    name: string;
    quantity: number;
    price: number;
    quantityDisplay: string;
    priceDisplay: string;
};

export type contributor = {
    name: string;
    contributions: number[];
};
